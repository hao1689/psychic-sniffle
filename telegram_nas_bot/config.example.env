# 複製這個檔為 config.env 後填入你的值,切勿把真正的 token 上傳到 git。

# 跟 @BotFather 申請的 bot token(必填)
TELEGRAM_BOT_TOKEN=請填入你的token

# 你的 Telegram user id(第一次可留空,用 /whoami 取得後再填)
ALLOWED_USER_ID=

# 允許 bot 操作的根目錄;所有指令都被鎖在這個目錄之內。
# 例如:/volume1/data 或 /volume1/photo
ALLOWED_ROOT=/volume1/data
